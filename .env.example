# Fernet 加密密钥 (base64, 32 bytes) — 生成方式: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
CIPHER_KEY=your_generated_key_here

# SMTP 邮件配置
SMTP_HOST=smtp.qq.com
SMTP_PORT=587
SMTP_USERNAME=your_email@qq.com
SMTP_PASSWORD=your_authorization_code
SMTP_FROM=your_email@qq.com
